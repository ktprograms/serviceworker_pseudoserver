{
  "name": "serviceworker_virtual_api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:watch": "npm run build -- --watch",
    "preview": "vite preview",
    "lint": "eslint . --ext js,cjs --report-unused-disable-directives --max-warnings 0",
    "lint:fix": "npm run lint -- --fix"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "vite": "^5.1.4"
  },
  "dependencies": {
    "uuid": "^10.0.0",
    "workbox-routing": "^7.0.0"
  }
}
